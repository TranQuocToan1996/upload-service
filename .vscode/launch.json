{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "mode": "debug",
            "request": "launch",
            "program": "${workspaceFolder}/cmd/api"
            // "program": "${workspaceFolder}/cmd/migrations"
        }
    ]
}